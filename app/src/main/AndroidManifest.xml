<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luciana.techpain">

    <application
        android:name="com.luciana.techpain.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cadastro" />
        <!--<activity android:name="com.luciana.techpain.Body1" /> NO XML REFERENCE-->
        <activity android:name=".BodyImage" />
        <activity android:name=".PerfilAdicao" />
        <activity android:name=".Ampere"></activity>
    </application>

</manifest>